interface FriendProfileProps {
  isMinimize: boolean;
}

function FriendProfile({ isMinimize }: FriendProfileProps) {
  return <div style={{ color: 'white' }}>지인페이지 프로필</div>;
}

export default FriendProfile;
