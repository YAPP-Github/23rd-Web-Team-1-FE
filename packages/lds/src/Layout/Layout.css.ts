import { style } from '@vanilla-extract/css';

export const container = style({
  position: 'relative',
  maxWidth: '72rem',
  minWidth: '36rem',
  width: '100%',
  height: '100%',
  margin: '0 auto',
});
